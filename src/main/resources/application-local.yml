spring:
  datasource:
    url: "${DB_URL:jdbc:mysql://localhost:3306/spring_boot_template?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}"
    username: "${DB_USERNAME:spring_boot_app}"
    password: "${DB_PASSWORD:hua12345}"
  data:
    redis:
      host: localhost
      port: 6379
      password: hua12345
      database: 0

logging:
  level:
    root: info
    com.chaoxing.template: debug
